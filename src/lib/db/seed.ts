// import { seedData } from "@/data";
// import { getDb } from "./db";
// import { schools } from "./schema";

// const seed = async () => {
//   const db = getDb();

//   const data = seedData;
//   await db.delete(schools);
//   const d = await db.insert(schools).values(data);
// };

// seed();
